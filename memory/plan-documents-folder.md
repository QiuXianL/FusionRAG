---
name: plan-documents-folder
description: 所有方案文档统一放在 plans/ 文件夹中
metadata:
  type: feedback
---

所有向用户说明的方案文档统一放在 `plans/` 文件夹中，以后新的方案文档也放在这里。

**Why:** 用户希望方案文档集中管理，不与项目根目录的其他文件混在一起。

**How to apply:** 写方案文档时，文件路径使用 `plans/<简短描述>.md`，不放在项目根目录。
